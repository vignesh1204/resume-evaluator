% backend/templates/template.tex (uses resume_old.cls style: name/address in preamble)
\documentclass{resume}

\usepackage[left=0.5in,top=0.3in,right=0.5in,bottom=0.3in]{geometry}
\usepackage{enumitem}

% tightitemize: compact bullets matching rSubsection style
\newlist{tightitemize}{itemize}{1}
\setlist[tightitemize]{
  label=$\cdot$,
  leftmargin=0em,
  itemsep=0pt,
  topsep=1pt,
  parsep=0pt,
  partopsep=0pt
}

% Override resume.cls spacing
\def\sectionskip{\vspace{2pt}}
\def\sectionlineskip{\vspace{4pt}}
\def\nameskip{\smallskip}
\def\addressskip{\vspace{1pt}}

% Replace rSection: no list env, just a group — zero extra vertical space
\renewenvironment{rSection}[1]{%
  \sectionskip
  \MakeUppercase{{\bf #1}}%
  \sectionlineskip
  \hrule
  \vspace{2pt}%
  \noindent\ignorespaces
}{%
  \par
}

\name{%%__NAME__%%}
\address{%%__CONTACT__%%}

\begin{document}

%%__BODY__%%

\end{document}
